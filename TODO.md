# TODO

- [x] Implement lifecycle-safe navigation in `lib/SplashScreen.dart` (cancel timer / mounted guard).
- [x] Clean up `lib/SplashScreen.dart` constants and formatting (no behavior change).
- [x] Implement a working `lib/HomeScreen.dart` UI (remove `throw UnimplementedError()`).
- [x] Remove unused imports and fix formatting in `lib/HomeScreen.dart`.

- [ ] Verify asset path correctness (`assets/logo.png` vs `assest/logo.png`) and update `pubspec.yaml` if required.
- [ ] Run `flutter analyze` and `flutter run` to validate.

